import React from 'react';

const contractTypes = {
    workContract: 'WorkContract',
    apartmentContract: 'ApartmentContract',
    btlContract: 'BTLContract',
    appendixAContract: 'AppendixAContract'
}

export const Contracts = (props: any) => {
    const { lockType } = props

    return(
        <>

        </>
    )
}